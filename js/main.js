function validarEmail() {
    // Obtém o valor do campo de email
    var email = document.getElementById('email').value;

    // Expressão regular para validar o formato do email
    var regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

    // Verifica se o email corresponde à expressão regular
    if (regex.test(email)) {
      alert('Email')
    }
}
  function validarEmail() {
    // Obtém o valor do campo de email
    var email = document.getElementById('email').value;

    // Expressão regular para validar o formato do email
    var regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

    // Verifica se o email corresponde à expressão regular
    if (regex.test(email)) {
      alert('Email válido!');
    } else {
      alert('Email inválido! Por favor, insira um email válido')
    }
}
  function validarEmail() {
    // Obtém o valor do campo de email
    var email = document.getElementById('email').value;

    // Expressão regular para validar o formato do email
    var regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

    // Verifica se o email corresponde à expressão regular
    if (regex.test(email)) {
      alert('Email válido!');
    } else {
      alert('Email inválido! Por favor, insira um email válido')
    }
}
  function validarEmail() {
    // Obtém o valor do campo de email
    var email = document.getElementById('email').value;

    // Expressão regular para validar o formato do email
    var regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

    // Verifica se o email corresponde à expressão regular
    if (regex.test(email)) {
      alert('Email válido!');
    } else {
      alert('Email inválido! Por Favor, insira um email válido')
    }
}
  function validarEmail() {
    // Obtém o valor do campo de email
    var email = document.getElementById('email').value;

    // Expressão regular para validar o formato do email
    var regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

    // Verifica se o email corresponde à expressão regular
    if (regex.test(email)) {
      alert('Email válido!');
    } else {
      alert('Email inválid0! Por favor, insira um email válido')
    }
}
  function validarEmail(){
    // Obtém o valor do campo de email
    var email = document.getElementById('email').value;

    // Expressão regular para validar o formato do email
    var regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

    // Verifica se o email corresponde à expressão regular
    if (regex.test(email)) {
      alert('Email válido!');
    } else {
      alert('Email inválido! Por favor, insira um email válido.');
    }
  }
